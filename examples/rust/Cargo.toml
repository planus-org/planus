[package]
name = "planus-example"
version = "0.2.0"
edition = "2021"

[dependencies]
planus = { version = "0.2.0", path = "../../planus" }
serde = { version = "1", default-features = false, features = ["derive", "std"] }

[dev-dependencies]
serde_json = "1.0.73"
